-- ============================================================
-- 0014 · 공유 글 "핵심 인사이트" 구조화 입력
--   자유 메모 대신 구조화된 회고를 저장한다(링크·직접작성 공통).
--   insight = {
--     core:           핵심 인사이트 (필수),
--     quote:          인상적인 문장,
--     interpretation: 인사이트 해석,
--     apply:          내가 바로 적용할 수 있는 것,
--     similar:        비슷한 사례,
--     questions:      [질문 리스트]
--   }
--   note/body 에는 core 를 그대로 넣어(목록 미리보기·검색 호환) 유지한다.
-- ============================================================
alter table public.shares add column if not exists insight jsonb;
